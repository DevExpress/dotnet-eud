---
title: Keyboard Navigation
---
# Keyboard Navigation

The Web Dashboard supports keyboard interaction in two modes: **Navigation mode** and **Interaction mode**.

## Navigation Mode

In Navigation mode, you can move focus between the following dashboard layout elements:

- Dashboard Title
- Tab Container page titles
- Groups
- Dashboard Items

| Action | Keys |
| --- | --- |
| Move focus between layout elements. | Tab / Shift+Tab |
| Switch to Interaction mode when a dashboard item is focused. | Enter |
| Exit Interaction mode. | Esc |

## Interaction Mode

Interaction mode lets you navigate within the focused dashboard item — through the item caption and item content.

Use the following keys for dashboard items:

| Action | Keys |
| --- | --- |
| Switch to Interaction mode. | Enter |
| Apply a card as a Master Filter. | Enter |
| Toggle a predefined interval in a Date Filter item. | Enter |
| Navigate between cards in a Card item. | Arrow keys |
| Move focus between elements in the dashboard item caption. | Tab / Shift+Tab |
| Exit Interaction mode. | Esc |

> [!NOTE]
> Keyboard navigation inside the item content is not available for the following dashboard items: Chart, Scatter Chart, Pie, Gauge, Treemap, Range Filter, and Map items (Choropleth Map, Geo Point Maps).

## Dialogs

The following dialogs support standard keyboard interaction:

- Data Inspector
- Export (PDF, Excel, Image)
- Set Predefined Intervals (Range Filter)
- Dashboard Parameters
