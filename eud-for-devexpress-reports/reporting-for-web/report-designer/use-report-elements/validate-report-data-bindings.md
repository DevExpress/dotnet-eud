---
title: Validate Report Data Bindings
author: Anna Vekhina
---
# Validate Report Data Bindings

After you assign a new data source to a loaded report, the report tries to automatically resolve all data bindings. You can check which the field names of your data source do not coincide with the report controls' bindings.

To do this, click the [Main Toolbar param($match) $path = $match.Groups[1].Value; if ($path -notmatch '^https?://' -and $path -notmatch '^~/' -and $path -notmatch '^\.\./\.\./') { '](' + '../' + $path + '.md)' } else { $match.Value } 's **Validate Bindings** command. Report controls with invalid bindings are marked with the ![](..\/..\/images/eurd-web-invalid-report-control-bindings-icon.png) icon in the [Design surface param($match) $path = $match.Groups[1].Value; if ($path -notmatch '^https?://' -and $path -notmatch '^~/' -and $path -notmatch '^\.\./\.\./') { '](' + '../' + $path + '.md)' } else { $match.Value } .

![](..\/..\/images/eurd-web-validation-bindings.png)
